/*
 * Copy this file to secrets.h and put your own values in.
 * secrets.h is gitignored and never leaves your machine.
 */

#pragma once

// Passphrase for the setup portal, where the password of your real network
// gets typed in. WPA2 is what keeps it off the air. Minimum 8 characters.
const char *const AP_PASS = "change-me";

// Login for the settings page once the clock is on a real network.
const char *const UI_USER = "admin";
const char *const UI_PASS = "change-me";
